# oscill 快速使用步骤

> 适用：Ubuntu 22.04 + Python 3.10.12 + Tektronix MSO4034（NI-VISA / USB）
> 项目路径：`/home/test/max_tool/oscill/oscill-0.1.0-linux`（示例路径，请替换为你的实际路径）

---

## 1. 环境准备（首次）

```bash
sudo apt install -y python3-pyqt5 python3-numpy python3-pip python3-venv
cd /home/test/max_tool/oscill/oscill-0.1.0-linux
python3 -m venv --system-site-packages .venv
source .venv/bin/activate
pip install "pyqtgraph>=0.13,<0.14"
pip install --no-deps .
```

> 以后每次使用只需：`source /home/test/max_tool/oscill/oscill-0.1.0-linux/.venv/bin/activate`

---

## 2. 识别示波器

### 方式一：命令行识别（推荐先做这步确认连接）

```bash
source .venv/bin/activate

# 列出所有 VISA 资源
oscill list-resources

# 用具体资源名识别示波器
oscill --resource "USB0::0x0699::0x0408::C012345::INSTR" identify
```

识别成功会输出类似：
```
TEKTRONIX,MSO4034,C012345,CF:91.1CT FV:1.38
```

> `0x0699` 是 Tektronix 的 USB VID，`0x0408` 是 MSO4034 的 PID，`C012345` 是仪器序列号。如果 `list-resources` 看不到该资源，检查 USB 线和示波器电源。

### 方式二：图形界面识别

1. 启动 GUI：`oscill-gui`
2. 在"设备连接"区的资源下拉框中输入或选择 `USB0::0x0699::0x0408::C012345::INSTR`
3. 点击 **扫描** 可自动列出资源，点击 **连接**
4. 连接成功后，身份信息栏显示示波器返回的 `*IDN?` 字符串

---

## 3. GUI 快速操作

| 操作 | 步骤 |
|------|------|
| **测量** | 勾选通道（如 CH1）→ 选择测量类型（平均电压/纹波/有效值等）→ 点击"开始测量"，每 1 秒刷新 |
| **波形** | 勾选通道 → 点击"读取波形"（单次）或"实时刷新"（每 500ms 更新） |
| **功耗** | 选择功耗任务（POWER-01B~06）和负载 → 点击"应用预设" → 开始测量，自动累计功率/能耗 |
| **导出** | 波形区"导出 CSV"；左侧"保存测试结果"导出 JSON 报告 |
| **切换语言** | 右上角下拉框选 中文 / English，即时生效无需重启 |

---

## 4. 常用命令速查

```bash
# 模拟模式（无需示波器，验证环境）
oscill --simulate identify
oscill --simulate --model tektronix-mso4034 measure --parameter VPP --source CH1

# 真实示波器
oscill list-resources
oscill --resource "USB0::0x0699::0x0408::C012345::INSTR" identify
oscill --resource "USB0::0x0699::0x0408::C012345::INSTR" --model tektronix-mso4034 measure --parameter MEAN --source CH1

# CLI 英文输出
OSCILL_LANGUAGE=en_US oscill list-resources
```

---

## 5. 常见问题

- **`No module named 'PyQt5'`**：未激活 venv，执行 `source .venv/bin/activate`
- **连接超时**：确认示波器已开机、USB 线已插好，`oscill list-resources` 能看到资源
- **详细说明**：见同目录《环境配置与使用说明.md》
