# oscill 示波器上位机改造执行计划

> 版本：v1.2 | 生成日期：2026-09-18 | 状态：已确认

## 1. 项目概述

本项目对现有 oscill 示波器上位机软件进行三项核心改造：①移除 matplotlib 波形显示模块；②以 PyQtGraph 替代实现高性能实时波形显示；③为整个上位机界面添加中英文双语切换支持。改造范围集中在 GUI 层（`oscill/gui.py`），底层 NI-VISA 连接、SCPI 数据采集、测量计算、功耗统计、CSV/JSON 导出等已验证功能**必须原样保留、零干扰**。最终交付完整可运行代码及一份详细的环境配置与使用说明文档。

**目标运行平台为 Linux + Ubuntu 22.04 LTS**，代码在 Windows 环境编写但不在 Windows 上运行或测试；所有运行时验证均在 Ubuntu 22.04 上完成。

## 2. 原始需求原文

> 以下为用户输入的原始需求，未经修改。

第 1 步：详细阅读我文件夹内所有的内容；

第 2 步：由于我已经完成了对于 NI-VISA 连接示波器将进行数据采集以及获取还有处理，所以在保证这些功能不报备干扰的情况下删去原本 matplotlib 进行示波器实时图像的显示的相关代码；

第 3 步：我不想要使用 matplotlib 进行图像显示，使用 PyQtGraph 替代 matplotlib 显示图像的功能，并完成对于示波器实时图像的显示（Ubuntu + Python + 示波器 NI-VISA 读取波形）；

第 4 步：将所有的软件上位机上的添加英文（就比如我支持语言包含 English 和中文，可以自由选择当前显示的支持语言）；

第 5 步：修改完代码后给我交付 1 份完整的代码；

第 6 步：给我详细的使用步骤以及环境配置并以一份 md 文档交付（我需要你帮助我检查安装的各个库是否会出现环境不兼容的问题，同时目前我并不需要禁用 usbtmc 内核模块，同样也可以完成对于示波器的连接与数据采集，这个我已经测试过了，所以不需要在 md 文档中提出）；

根据 skill 给我交付一份执行计划

### 需求冲突项（如有）

无直接冲突。但第 3 步存在一个**架构约束需要用户确认（已确认）**：当前 GUI 基于 Tkinter，而 PyQtGraph 严格依赖 Qt（PyQt5/PySide6）事件循环，无法在纯 Tkinter 窗口中原生嵌入。用户已同意将 GUI 整体迁移到 PyQt5。详见第 3.2 节方案选型。

## 3. 工程前置约束

### 3.1 技术栈与环境



* **语言 / 框架**：Python **3.10.12**（Ubuntu 22.04 系统自带 `python3` 实测版本）；GUI 框架从 Tkinter 迁移至 **PyQt5**；绘图引擎使用 **PyQtGraph**

* **目标运行平台**：**Linux + Ubuntu 22.04 LTS（Jammy Jellyfish）**，x86\_64 架构

* **项目部署路径（Ubuntu 实际路径）**：


  * 项目根目录：`/home/test/max_tool/oscill/oscill-0.1.0-linux`

  * 虚拟环境目录：`/home/test/max_tool/oscill/oscill-0.1.0-linux/.venv`

  * 激活命令：`source /home/test/max_tool/oscill/oscill-0.1.0-linux/.venv/bin/activate`

* **开发平台说明**：代码在 Windows 环境编写，**不在 Windows 上运行或测试**；Windows 侧仅执行语法检查、静态分析等不依赖 Qt/NI-VISA 的验证；所有运行时功能验证均在 Ubuntu 22.04 上完成

* **运行环境**：Ubuntu 22.04 LTS，Python 3.10.12，NI-VISA 2026 Q3 已安装并验证，Python 虚拟环境带 `--system-site-packages`

* **关键依赖**：


  * `pyvisa`（使用系统已安装版本，通过 `--system-site-packages` 继承，不强制升级）

  * `PyQt5 >= 5.15`（Qt GUI 框架，PyQtGraph 的宿主；Ubuntu 22.04 系统包 `python3-pyqt5` 版本为 5.15.6，满足要求）

  * `pyqtgraph >= 0.13`（高性能实时绘图）

  * `numpy`（PyQtGraph 依赖，波形数据处理；Ubuntu 22.04 系统包 `python3-numpy` 版本为 1.21.x，满足 >=1.20 要求）

* **代码基线**：oscill v0.1.0，项目根目录 `oscill-0.1.0-linux/`

* **已验证不变功能（硬性保护范围）**：


  * NI-VISA 设备连接与断开（`transport.py` 的 `VisaTransport`、`list_visa_resources`）

  * SCPI 命令发送与响应解析（`controller.py` 的 `Oscilloscope`、`TektronixMSO4034`）

  * 波形数据采集与二进制解析（`read_waveform`，含 RIBinary 解码、X/Y 轴工程换算）

  * 标量测量（VPP/MEAN/RMS/MIN/MAX/FREQUENCY，`services.py` 的 `collect_configured_measurements`）

  * 同步功耗统计（`acquire_synchronized_power_statistics`）

  * CSV/JSON 数据导出（`save_configured_waveforms_csv`、测试结果 JSON 序列化）

  * 后台线程串行执行（`worker.py` 的 `SerialWorker`）

  * CLI 命令行工具（`cli.py`）

### 3.2 选定方案与备选



| 需求项                     | 选定方案                                                                                              | 备选方案                                                                                                | 选择理由                                                                                                                                                                        |
| ----------------------- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PyQtGraph 替代 matplotlib | **将 GUI 整体从 Tkinter 迁移到 PyQt5**，在 Qt 窗口中原生使用 PyQtGraph 的 `PlotWidget`/`GraphicsLayoutWidget`      | 方案 B：在 Tkinter 中通过 `winfo_id()` + `QWindow.fromWinId()` 嵌入 Qt 窗口；方案 C：弹出独立 Qt 子窗口显示波形，主界面保留 Tkinter | PyQtGraph 必须运行在 Qt 事件循环中，Tkinter 与 Qt 双事件循环桥接在 Linux 上不稳定且维护成本高；整体迁移到 PyQt5 是最干净、性能最好、长期可维护的方案。底层逻辑层（controller/services/transport/worker）与 GUI 解耦，迁移仅触及 `gui.py`。**用户已确认** |
| 中英文国际化                  | **轻量字典映射方案**：定义 `Translator` 类，维护 `zh_CN` / `en_US` 两套文本字典，通过 `tr(key)` 方法取值，语言切换时遍历所有可更新控件重新设置文本 | 方案 B：使用 Python 标准库 `gettext` + `.po/.mo` 文件                                                         | 应用规模中等（单窗口 GUI），字典映射方案零额外构建步骤、易于维护、切换语言时可动态刷新界面；gettext 需要编译 `.mo` 文件且动态切换较繁琐。**用户已确认**                                                                                     |
| 实时波形刷新                  | PyQtGraph `PlotDataItem.setData()` 增量更新，配合 QTimer 定时触发采集；多通道使用多个 `PlotItem` 共享 X 轴                | 使用 `GraphicsLayoutWidget` 手动布局                                                                      | `setData()` 只更新数据不重建图形，帧率高；共享 X 轴保证多通道时间对齐                                                                                                                                  |

### 3.3 其他约束



* **底层功能零干扰（最高优先级硬约束）**：`controller.py`、`services.py`、`transport.py`、`worker.py` 四个模块的代码逻辑、公共 API、数据结构（`Waveform`、`Measurement`、`MeasurementRequest`、`PowerWindowStatistics` 等）**一律不得修改**；GUI 重写只能调用这些模块的现有接口，不得反向改动其签名或行为。唯一例外是 `cli.py` 中两处中文字符串的国际化（T-07），改动仅限字符串展示层，不触碰命令逻辑

* **入口点不变**：`oscill-gui` 命令仍指向 `oscill.gui:main`，`oscill` 命令仍指向 `oscill.cli:main`，用户启动方式不变

* **目标平台限定 Ubuntu 22.04**：所有新增依赖、路径、系统包名称、安装命令均以 Ubuntu 22.04 LTS + Python 3.10.12 为基准；不考虑 Windows/macOS 兼容性；代码中不得引入 Windows 专有路径或 API

* **所有文档与脚本使用实际路径**：统一使用 `/home/test/max_tool/oscill/oscill-0.1.0-linux`，不使用 `~` 或占位路径

* **usbtmc 不处理**：用户已验证无需禁用 `usbtmc` 内核模块即可正常连接，文档中不提及该步骤

* **pyvisa 版本**：尊重用户现有环境（可能使用系统级旧版 pyvisa），不强制升级；新代码不使用 pyvisa 高版本专有 API

* **i18n 方案确认**：采用轻量字典映射（非 gettext），新建 `oscill/i18n.py`，内置 `zh_CN`/`en_US` 两套字典，支持运行时动态切换、界面即时刷新，无需重启程序

* **向后兼容**：`SimulatedTransport` 模拟模式继续可用，便于无示波器时开发测试；默认语言为中文

* **无法本地运行验证**：由于开发环境（Windows）无 NI-VISA、无示波器、无 PyQt5 运行时，代码完成后需在 Ubuntu 22.04（Python 3.10.12）上由用户执行真实环境验证；交付前在 Windows 侧完成全部可做的静态检查（语法编译、导入检查、代码审查、跨平台路径审查）

## 4. 任务拆解

### 4.1 任务总览



| 子任务编号 | 子任务名称                                       | 优先级 | 依赖               | 涉及模块 / 文件                                    |
| ----- | ------------------------------------------- | --- | ---------------- | -------------------------------------------- |
| T-01  | 项目代码全面梳理与 matplotlib 依赖定位                   | P0  | 无                | oscill/gui.py, pyproject.toml                |
| T-02  | 移除 matplotlib 相关代码与依赖声明                     | P0  | T-01             | oscill/gui.py, pyproject.toml                |
| T-03  | GUI 框架从 Tkinter 迁移到 PyQt5（界面骨架）             | P0  | T-02             | oscill/gui.py（重写）                            |
| T-04  | 集成 PyQtGraph 波形显示组件                         | P0  | T-03             | oscill/gui.py                                |
| T-05  | 实现实时波形刷新与单次读取逻辑                             | P0  | T-04             | oscill/gui.py                                |
| T-06  | 实现中英文国际化框架与语言切换控件                           | P1  | T-03             | oscill/gui.py, oscill/i18n.py（新增）            |
| T-07  | 全面翻译 UI 文本为中英文双语                            | P1  | T-06             | oscill/gui.py, oscill/i18n.py, oscill/cli.py |
| T-08  | 依赖版本兼容性检查与锁定（Ubuntu 22.04 / Python 3.10.12） | P1  | T-02, T-04       | pyproject.toml                               |
| T-09  | 编写环境配置与使用说明 MD 文档（Ubuntu 22.04）             | P1  | T-08             | 使用说明.md（新增）                                  |
| T-10  | 整体功能验证与回归测试（分两阶段）                           | P0  | T-05, T-07, T-08 | 全部模块                                         |

### 4.2 子任务详情

#### T-01：项目代码全面梳理与 matplotlib 依赖定位



* **优先级**：P0

* **依赖**：无

* **目标**：精确定位所有 matplotlib 引用点，确认底层逻辑层不受影响

* **操作步骤**：

1. 已完成：通读 `oscill/` 下全部 7 个 Python 文件

2. 已完成：确认 matplotlib 仅出现在 `gui.py` 中（第 242-254 行导入与画布创建、第 640-642 行 `plot_available` 检查、第 668-722 行 `_plot_waveforms` 方法、第 739-743 行 `plot_available` 检查）

3. 已完成：确认 `pyproject.toml` 中 `gui = ["matplotlib>=3.8"]` 及 `dev` 依赖中的 matplotlib

4. 已完成：确认 `controller.py`/`services.py`/`transport.py`/`worker.py`/`cli.py` 无 matplotlib 引用

* **涉及文件 / 模块**：oscill/gui.py, pyproject.toml

* **验收标准**：matplotlib 引用点清单完整，底层模块确认无依赖

#### T-02：移除 matplotlib 相关代码与依赖声明



* **优先级**：P0

* **依赖**：T-01

* **目标**：彻底清除 matplotlib 代码与依赖，为 PyQtGraph 集成腾出空间

* **操作步骤**：

1. 删除 `gui.py` 中 `from matplotlib.backends.backend_tkagg import FigureCanvasTkAgg` 和 `from matplotlib.figure import Figure` 导入

2. 删除 `_build_ui` 中波形区域的 Figure/Axes/Canvas 创建逻辑（第 240-254 行）

3. 删除 `self.plot_available` 标志及相关条件判断

4. 删除 `_plot_waveforms` 方法中的全部 matplotlib 绘图代码（保留方法签名，后续替换为 PyQtGraph 实现）

5. 删除 `_read_waveform` 和 `_toggle_waveform_polling` 中的 `plot_available` 检查

6. 修改 `pyproject.toml`：将 `gui = ["matplotlib>=3.8"]` 替换为 `gui = ["PyQt5>=5.15", "pyqtgraph>=0.13", "numpy"]`；从 `dev` 依赖中移除 matplotlib

* **涉及文件 / 模块**：oscill/gui.py, pyproject.toml

* **验收标准**：全局搜索 `matplotlib` 无残留；`pyproject.toml` 依赖已更新

#### T-03：GUI 框架从 Tkinter 迁移到 PyQt5（界面骨架）



* **优先级**：P0

* **依赖**：T-02

* **目标**：用 PyQt5 重建与原 Tkinter 版功能等价的 GUI 骨架，包含设备连接区、示波器控制区、实时测量卡片区、波形区、操作日志区、状态栏

* **操作步骤**：

1. 新建 `OscilloscopeApp(QMainWindow)` 类替代原 `tk.Tk` 子类

2. 使用 `QWidget` + `QVBoxLayout`/`QHBoxLayout`/`QGridLayout` 重建布局

3. 设备连接区：`QComboBox`（资源名）+ `QPushButton`（扫描 / 连接 / 断开）+ `QLabel`（设备身份）

4. 示波器控制区：`QPushButton`（自动量程 / 开始采集 / 停止采集）+ `QCheckBox`（通道启用）+ `QLineEdit`（垂直刻度）+ 功耗测试任务下拉框 + 负载状态下拉框 + Device Part No./FW Version 输入框 + 功率上限输入框 + 结果标签

5. 实时测量区：4 个通道卡片（`QGroupBox`），每个含测量类型 `QComboBox`、A/V 系数 `QLineEdit`、Min/Max `QLineEdit`、数值 `QLabel`、状态 `QLabel`

6. 波形区：占位 `QWidget`，后续 T-04 嵌入 PyQtGraph

7. 操作日志区：`QTextEdit`（只读）+ 展开 / 收起按钮

8. 状态栏：`QStatusBar`

9. 保留原有的 `SerialWorker` 后台线程机制，通过 `QTimer` 轮询 `Future` 结果替代 Tkinter 的 `after()`

10. 保留 `_submit` / `_watch_future` 异步模式，适配 Qt 事件循环

* **涉及文件 / 模块**：oscill/gui.py（整体重写）

* **验收标准**：GUI 可启动，所有控件布局与原版本功能对应，无 matplotlib 依赖

#### T-04：集成 PyQtGraph 波形显示组件



* **优先级**：P0

* **依赖**：T-03

* **目标**：在波形区嵌入 PyQtGraph，支持多通道分图显示、共享 X 轴、网格、零轴、通道颜色区分

* **操作步骤**：

1. 导入 `pyqtgraph`

2. 创建 `pg.GraphicsLayoutWidget` 作为波形容器

3. 根据选中通道数动态创建 `PlotItem`，垂直排列，共享 X 轴（`setXLink`）

4. 每个 `PlotItem` 添加 `PlotDataItem` 用于波形曲线，颜色使用原 `_CHANNEL_COLORS` 配色

5. 设置 Y 轴标签（Voltage/Current/Amplitude + 工程单位）、X 轴标签（Time + 工程单位）

6. 添加零轴参考线（`pg.InfiniteLine`）

7. 启用网格，设置抗锯齿

8. 实现 `_plot_waveforms` 方法：接收 `dict[str, Waveform]`，计算工程比例尺（复用原 `_engineering_scale` 逻辑），调用 `setData()` 更新每条曲线

9. 在图上标注峰峰值和采样数（使用 `pg.TextItem` 或 `PlotItem.setTitle`）

* **涉及文件 / 模块**：oscill/gui.py

* **验收标准**：模拟模式下可显示波形；多通道布局正确；颜色与原版本一致

#### T-05：实现实时波形刷新与单次读取逻辑



* **优先级**：P0

* **依赖**：T-04

* **目标**：实现 "读取当前波形"（单次）和 "开始实时波形"（连续刷新）功能，复用原有的 `acquire_configured_waveforms` 服务层函数

* **操作步骤**：

1. 实现 `_read_waveform`：调用 `acquire_configured_waveforms` 获取波形，更新 `last_waveforms`，调用 `_plot_waveforms` 绘制

2. 实现 `_toggle_waveform_polling`：切换实时波形状态，更新按钮文本

3. 实现 `_poll_waveforms`：使用 `QTimer`（间隔 500ms）触发采集，通过 `SerialWorker` 异步执行，完成后调用 `_plot_waveforms` 并调度下一次

4. 保留 `_waveform_inflight` 防重入机制和 `_waveform_generation` 版本号机制（防止过期回调更新界面）

5. 实现 `_save_waveform`：保存 `last_waveforms` 为 CSV（复用 `save_configured_waveforms_csv`）

* **涉及文件 / 模块**：oscill/gui.py

* **验收标准**：单次读取正常；实时波形流畅刷新（>=2fps）；停止后不再采集；CSV 导出正常

#### T-06：实现中英文国际化框架与语言切换控件



* **优先级**：P1

* **依赖**：T-03

* **目标**：建立轻量 i18n 机制，支持运行时切换语言

* **操作步骤**：

1. 新建 `oscill/i18n.py`，定义 `Translator` 类：

* 内置 `zh_CN` 和 `en_US` 两个文本字典

* `tr(key)` 方法根据当前语言返回对应文本

* `set_language(lang)` 方法切换语言并触发 `languageChanged` 信号（可基于 `PyQt5.QtCore.QObject` + `pyqtSignal`，或简单回调）

* 支持键嵌套（如 `"connection.title"`）

1. 在 `OscilloscopeApp` 中实例化 `Translator`，默认语言 `zh_CN`

2. 在界面顶部或菜单栏添加语言切换 `QComboBox`（选项：中文 / English）

3. 实现 `_retranslate_ui()` 方法：遍历所有需要翻译的控件，重新设置文本

4. 语言切换时发出信号，连接到 `_retranslate_ui()`

* **涉及文件 / 模块**：oscill/gui.py, oscill/i18n.py（新增）

* **验收标准**：切换语言后所有界面文本立即更新；默认中文

#### T-07：全面翻译 UI 文本为中英文双语



* **优先级**：P1

* **依赖**：T-06

* **目标**：将 GUI 中所有用户可见文本提取为 i18n 键，并提供完整的中英文翻译

* **操作步骤**：

1. 提取所有控件文本：窗口标题、GroupBox 标题、按钮文本、标签文本、下拉框选项、占位符、状态栏文本、日志消息、错误对话框标题与内容

2. 翻译范围包括但不限于：

* 设备连接区："设备连接"、"VISA 资源名"、"扫描"、"连接"、"断开"、"未连接"、"正在连接…"

* 示波器控制区："示波器控制"、"自动量程"、"开始采集"、"停止采集"、"启用测量通道"、"垂直刻度 (V/div)"、"应用到已选通道"、"SSD 功耗测试任务"、"负载 / 事件状态"、"应用任务预设"、"功率上限 (W)"、"保存测试结果 JSON"

* 测量区："实时测量"、"开始连续测量"、"停止连续测量"、各测量类型（平均电压、纹波峰峰值、有效值电压、最小电压、最大电压、平均电流、峰值电流、电流纹波、频率）、"A/V"、"Min"、"Max"、"TBD"、"PASS"、"FAIL"、"N/A：无有效信号"、"未选择"

* 波形区："波形"、"读取当前波形"、"开始实时波形"、"停止实时波形"、"保存 CSV"、"Time (s)"、"Voltage (V)"

* 日志区："操作日志"、"展开日志"、"收起日志"

* 功耗统计："同步功率：等待电压 / 电流测量"、"持续统计：等待开始"、"判定：TBD（请填写设计限值）" 等

* 对话框：所有 `QMessageBox` 的标题和内容

* CLI 中的中文文本（`cli.py` 第 30、38 行，仅字符串改动）

1. 在 `i18n.py` 中为每个键提供准确的英文翻译

2. 确保动态生成的文本（如状态字符串、测量结果格式）也支持双语

* **涉及文件 / 模块**：oscill/gui.py, oscill/i18n.py, oscill/cli.py

* **验收标准**：中文模式下文本与原版本一致；英文模式下所有文本均为正确英文；无遗漏的硬编码中文

#### T-08：依赖版本兼容性检查与锁定（Ubuntu 22.04 / Python 3.10.12）



* **优先级**：P1

* **依赖**：T-02, T-04

* **目标**：确认 PyQt5、pyqtgraph、numpy、pyvisa 在 Ubuntu 22.04 + Python 3.10.12 环境下的兼容性，锁定合理版本范围，输出经核实的兼容组合

* **操作步骤**：

1. 确认 Python 版本为 **3.10.12**（用户实测，满足 `requires-python >= 3.10`）

2. 核实 Ubuntu 22.04 系统包版本：`python3-pyqt5` = 5.15.6、`python3-numpy` = 1.21.x，均满足要求

3. 确认 pyqtgraph >=0.13 与 PyQt5 5.15 + Python 3.10.12 的兼容性（pyqtgraph 0.13 官方支持 PyQt5 5.15、Python 3.8+）

4. 确认 numpy 版本与 pyqtgraph 的兼容性（pyqtgraph 要求 numpy >=1.20，系统 1.21.x 满足）

5. 确认系统 pyvisa 版本与代码兼容性（代码仅使用基础 `ResourceManager`、`open_resource`、`query`、`write`、`query_binary_values`、`clear`、`close`、`list_resources`，这些 API 在 pyvisa 1.10+ 均稳定）

6. 在 `pyproject.toml` 中锁定依赖版本范围：`PyQt5>=5.15,<6`、`pyqtgraph>=0.13,<0.14`、`numpy>=1.20`

7. 提供两种 PyQt5 安装路径并在文档中说明：①apt 系统包（`sudo apt install python3-pyqt5 python3-pyqt5.qtsvg`，配合 `--system-site-packages` venv 继承）；②pip 安装（`pip install PyQt5`）；明确两种路径不混用

8. 跨平台审查：检查新代码中无 Windows 专有路径（如反斜杠、`C:\`）、无平台专有 API，文件路径统一使用 `pathlib` 或 `os.path`，文件读写显式 `encoding="utf-8"`

* **涉及文件 / 模块**：pyproject.toml

* **验收标准**：依赖版本范围与 Ubuntu 22.04 系统包 /pip 可获取版本一致；文档给出经核实的兼容组合；代码无平台专有写法

#### T-09：编写环境配置与使用说明 MD 文档（Ubuntu 22.04）



* **优先级**：P1

* **依赖**：T-08

* **目标**：交付一份针对 Ubuntu 22.04 LTS + Python 3.10.12 的详细 MD 文档，涵盖环境配置、依赖安装、使用步骤、常见问题

* **操作步骤**：

1. 文档结构：

* 环境要求（明确 Ubuntu 22.04 LTS、Python 3.10.12、NI-VISA 2026 Q3 已安装前提）

* 依赖安装步骤（PyQt5 提供 apt 系统包与 pip 两种方式、pyqtgraph/numpy 安装方式，说明 apt 与 pip 不混用原则）

* 虚拟环境创建（强调 `--system-site-packages`，使用实际路径 `/home/test/max_tool/oscill/oscill-0.1.0-linux`）

* oscill 包安装

* 验证安装（模拟模式测试）

* 连接示波器与数据采集步骤

* GUI 使用指南（连接、测量、波形、功耗测试、导出、语言切换）

* 依赖兼容性说明（各库版本要求与 Ubuntu 22.04 + Python 3.10.12 已核实兼容组合）

* 常见问题排查

1. **不包含** usbtmc 内核模块禁用步骤（用户明确要求）

2. 包含 PyQt5 在 Ubuntu 22.04 上的系统包安装方式（`sudo apt install python3-pyqt5 python3-pyqt5.qtsvg`）

3. 包含 pyqtgraph 的 pip 安装方式

4. 包含语言切换操作说明

5. 包含阶段 B 运行验证清单，供用户首跑时逐项确认

6. 所有命令统一使用实际项目路径 `/home/test/max_tool/oscill/oscill-0.1.0-linux`

* **涉及文件 / 模块**：使用说明.md（新增，放在项目根目录）

* **验收标准**：文档完整覆盖从环境搭建到日常使用的全流程；所有命令针对 Ubuntu 22.04 + Python 3.10.12 可直接复制执行；无 usbtmc 相关内容

#### T-10：整体功能验证与回归测试（分两阶段）



* **优先级**：P0

* **依赖**：T-05, T-07, T-08

* **目标**：验证所有功能正常，底层采集功能未受干扰。由于开发环境（Windows）无法运行 Qt/NI-VISA，验证分为 Windows 静态验证和 Ubuntu 22.04 运行验证两阶段

* **操作步骤**：

  **阶段 A：Windows 侧静态验证（交付前必须完成）**

  **阶段 B：Ubuntu 22.04 侧运行验证（用户执行，文档提供验证清单）**

  8\. 进入项目目录并激活 venv：



```
cd /home/test/max\_tool/oscill/oscill-0.1.0-linux

source .venv/bin/activate

python3 --version   # 确认 3.10.12
```



1. 语法编译检查：`python -m py_compile oscill/*.py` 全部通过

2. 无 Qt 依赖的模块导入检查：`python -c "import oscill.cli, oscill.controller, oscill.services, oscill.transport, oscill.worker"` 通过

3. 模拟模式 CLI 测试：`python -m oscill.cli --simulate identify` 和 `measure` 正常（SimulatedTransport 不依赖硬件）

4. i18n 模块单元测试：验证 `zh_CN`/`en_US` 字典键完全一致、无缺失键、无重复键

5. 全局搜索确认无 `matplotlib`、`tkinter` 残留导入

6. 跨平台审查：确认无 Windows 专有路径 / API，文件编码统一 UTF-8

7. 文件对比确认 `controller.py`/`services.py`/`transport.py`/`worker.py` 零改动，`cli.py` 仅字符串改动

8. GUI 启动测试：`python -m oscill.gui` 可正常启动，无导入错误

9. 模拟模式 GUI 测试：验证测量、波形显示、连续采集、CSV 导出

10. 语言切换测试：中英文切换后所有文本正确更新

11. 实时波形测试：确认 PyQtGraph 刷新流畅，无内存泄漏

12. 真实设备回归测试（核心）：连接示波器验证 NI-VISA 通信、`list-resources`、`identify`、波形采集（`read_waveform`）、标量测量、功耗统计、JSON/CSV 导出，与改造前行为逐项对比

* **涉及文件 / 模块**：全部模块

* **验收标准**：阶段 A 全部通过方可交付；阶段 B 验证清单写入使用文档，由用户在 Ubuntu 22.04 + Python 3.10.12 上确认；真实设备回归测试中底层采集功能与改造前完全一致

## 5. 代码变更规则

### 5.1 变更总览



| 子任务  | 变更类型    | 变更范围                                            | 影响面          | 兼容性                      |
| ---- | ------- | ----------------------------------------------- | ------------ | ------------------------ |
| T-02 | 删除 + 修改 | oscill/gui.py（matplotlib 代码）、pyproject.toml（依赖） | 仅 GUI 波形显示模块 | 不兼容（matplotlib 移除）       |
| T-03 | 重写      | oscill/gui.py（整体）                               | GUI 全部控件     | 不兼容（Tkinter→PyQt5），入口点不变 |
| T-04 | 新增      | oscill/gui.py（PyQtGraph 集成）                     | 波形显示模块       | 新增功能                     |
| T-05 | 修改      | oscill/gui.py（波形刷新逻辑）                           | 波形采集与显示      | 内部实现替换，外部行为不变            |
| T-06 | 新增      | oscill/i18n.py（新增）、oscill/gui.py                | 全部 UI 文本     | 新增功能，默认中文行为不变            |
| T-07 | 修改      | oscill/gui.py、oscill/i18n.py、oscill/cli.py      | 全部用户可见文本     | 中文模式下向后兼容                |
| T-08 | 修改      | pyproject.toml                                  | 依赖声明         | 依赖替换                     |
| T-09 | 新增      | 使用说明.md                                         | 文档           | 新增                       |

### 5.2 功能移除校验（matplotlib 移除）

> 需求包含移除 matplotlib 波形显示功能，必须填写本节。



* **待移除功能**：基于 matplotlib 的示波器波形实时显示（含 FigureCanvasTkAgg 嵌入、axes 绘图、tight\_layout 等）

* **代码依赖检查**：


  * 调用方：`gui.py` 中 `_build_ui`（创建画布）、`_read_waveform`（检查 plot\_available）、`_plot_waveforms`（执行绘图）、`_toggle_waveform_polling`（检查 plot\_available）、`_poll_waveforms`（调用 `_plot_waveforms`）

  * 共享逻辑：`_engineering_scale` 方法（工程单位换算）被 matplotlib 绘图和测量显示共用，**必须保留**，仅移除 matplotlib 专用部分

  * 数据依赖：`last_waveforms: dict[str, Waveform]` 被 matplotlib 绘图和 CSV 导出共用，**必须保留**

  * 其他模块：`controller.py`、`services.py`、`transport.py`、`worker.py`、`cli.py`、`__init__.py` 均无 matplotlib 导入或调用

* **删除影响评估**：matplotlib 仅用于 GUI 波形渲染，删除后波形显示功能暂时不可用（T-04 会用 PyQtGraph 替代）；CSV 导出、测量、功耗统计等功能不受影响；`_engineering_scale` 和 `last_waveforms` 保留供 PyQtGraph 复用

* **校验方式**：


  * 全局搜索 `matplotlib`、`FigureCanvasTkAgg`、`Figure(`、`self.axes`、`self.figure`、`self.canvas`，确认无残留

  * 运行 `python -c "import oscill.gui"` 确认无 ImportError（Ubuntu 侧）

  * 运行 `python -m oscill.cli --simulate identify` 确认 CLI 不受影响

  * 确认 `save_configured_waveforms_csv` 仍可正常调用

### 5.3 通用原则



1. 所有代码变更不得破坏原有项目底层功能（NI-VISA 连接、数据采集、测量计算、功耗统计、CSV/JSON 导出）

2. `controller.py`、`services.py`、`transport.py`、`worker.py` 的公共 API 和数据结构**零改动**；`cli.py` 仅允许中文字符串国际化

3. 入口点 `oscill` 和 `oscill-gui` 保持不变

4. 新增代码遵循项目现有代码风格（类型注解、`from __future__ import annotations`、dataclass 使用等）

5. 新增 `i18n.py` 模块遵循项目包结构，通过 `oscill.i18n` 导入

6. 所有用户可见文本必须经过 i18n 层，不得硬编码中文或英文

7. 代码面向 Ubuntu 22.04 + Python 3.10.12，使用跨平台路径处理（`pathlib`/`os.path`），文件读写显式 `encoding="utf-8"`，不引入平台专有 API

## 6. 风险与校验项



| 风险编号 | 风险描述                                                                                 | 风险等级 | 关联任务             | 校验方式                                                                                      | 校验时机            |
| ---- | ------------------------------------------------------------------------------------ | ---- | ---------------- | ----------------------------------------------------------------------------------------- | --------------- |
| R-01 | Tkinter→PyQt5 迁移过程中布局错乱，控件重叠或缺失                                                      | 中    | T-03             | 启动 GUI 目视检查所有区域；对比原版本功能清单逐项验证                                                             | 开发中             |
| R-02 | PyQtGraph 多通道共享 X 轴时布局异常或 Y 轴标签被裁剪                                                   | 中    | T-04             | 测试 1/2/3/4 通道场景；调整 `GraphicsLayout` 间距                                                    | 开发中             |
| R-03 | 实时波形刷新时内存泄漏（曲线对象未复用，反复创建）                                                            | 高    | T-05             | 长时间运行（>5 分钟）监控内存；确保使用 `setData()` 而非 `plot()`                                             | 提测前             |
| R-04 | Qt 事件循环与 `SerialWorker` 线程池交互导致界面卡顿或死锁                                               | 高    | T-03, T-05       | 使用 `QTimer` 轮询 Future 而非阻塞等待；验证连续测量 + 实时波形同时运行                                            | 提测前             |
| R-05 | 语言切换后动态生成的文本（如状态字符串、日志）未更新                                                           | 中    | T-07             | 切换语言后触发各类操作，检查所有动态文本；代码审查所有字符串拼接                                                          | 提测前             |
| R-06 | PyQt5 与系统 pyvisa 在 `--system-site-packages` venv 中冲突                                 | 中    | T-08             | 在干净 venv 中完整安装测试；检查 `pip check` 无依赖冲突                                                     | 提测前             |
| R-07 | Ubuntu 22.04 系统 python3-pyqt5 已核实为 5.15.6（满足），但若系统为最小化安装或曾混用 pip/apt 安装 PyQt5，可能版本错乱 | 低    | T-08             | `apt show python3-pyqt5` 确认版本；统一采用一种安装路径；异常时 `pip install --force-reinstall PyQt5`        | 提测前 + Ubuntu 验证 |
| R-08 | 迁移过程中误改底层模块，导致 NI-VISA 采集功能回归                                                        | 高    | T-03             | 文件对比确认 controller/services/transport/worker 零改动；模拟模式全功能测试；Ubuntu 22.04 真实设备逐项对比改造前行为      | 提测前 + Ubuntu 验证 |
| R-09 | pyqtgraph 在无显示器的 Ubuntu Server 环境下无法初始化 Qt                                           | 低    | T-04             | 文档中说明需要桌面环境（Ubuntu 22.04 Desktop）；提供 `QT_QPA_PLATFORM=offscreen` 调试方案                     | 文档阶段            |
| R-10 | 开发在 Windows、运行在 Ubuntu 22.04，跨平台问题（路径、编码、大小写、系统库）无法在开发时发现                            | 高    | T-03, T-08, T-10 | 代码统一使用 `pathlib`/`os.path`；显式 `encoding="utf-8"`；Windows 侧静态审查；文档提供完整 Ubuntu 验证清单，由用户首跑确认 | 开发中 + Ubuntu 首跑 |
| R-11 | Ubuntu 22.04 系统 pyqt5/numpy 与 pip 安装的 pyqtgraph 版本错配，导致导入冲突                          | 中    | T-08             | 文档明确 apt 与 pip 两种安装路径不混用；`pip check` 验证；优先推荐 apt 装 PyQt5 + pip 装 pyqtgraph 的组合            | 提测前 + Ubuntu 验证 |
| R-12 | GUI 重写后调用底层服务的参数顺序 / 关键字与原签名不一致，导致采集异常                                               | 高    | T-03, T-05       | 严格对照 `services.py` 函数签名调用；代码审查每一处 `acquire_*`/`collect_*`/`save_*` 调用；模拟模式验证数据链路          | 开发中             |

### 关键校验清单

**阶段 A：Windows 侧静态验证（交付前）**



* [ ] 全局搜索 `matplotlib`、`tkinter` 无残留

* [ ] `python -m py_compile oscill/*.py` 全部通过

* [ ] 无 Qt 依赖模块导入检查通过（cli/controller/services/transport/worker）

* [ ] `python -m oscill.cli --simulate identify` 输出正确

* [ ] i18n 中英文字典键完全一致、无缺失

* [ ] 无 Windows 专有路径 / API，文件读写显式 UTF-8

* [ ] `controller.py`/`services.py`/`transport.py`/`worker.py` 零改动（仅 cli.py 允许字符串改动）

* [ ] 所有底层服务调用签名与 services.py/controller.py 一致

**阶段 B：Ubuntu 22.04 侧运行验证（用户执行，Python 3.10.12）**



* [ ] 进入 `/home/test/max_tool/oscill/oscill-0.1.0-linux` 并激活 venv，`python3 --version` 为 3.10.12

* [ ] `python -m oscill.gui` 可正常启动

* [ ] 模拟模式下测量、波形、连续采集、CSV 导出均正常

* [ ] 中英文切换后所有文本正确更新

* [ ] 实时波形连续运行 5 分钟无内存泄漏、无卡顿

* [ ] `pip check` 无依赖冲突

* [ ] **真实示波器：NI-VISA 连接、identify、波形采集、标量测量、功耗统计、JSON/CSV 导出与改造前一致**

* [ ] 使用说明文档完整且无 usbtmc 相关内容

## 7. 交付物清单



| 交付物                | 类型 | 关联任务               | 交付标准                                                                      |
| ------------------ | -- | ------------------ | ------------------------------------------------------------------------- |
| 完整修改后代码（oscill 包）  | 代码 | T-02 \~ T-07       | 所有 .py 文件可直接运行，功能完整                                                       |
| oscill/gui.py（重写）  | 代码 | T-03 \~ T-05, T-07 | PyQt5 + PyQtGraph 实现，支持实时波形和双语                                            |
| oscill/i18n.py（新增） | 代码 | T-06, T-07         | 轻量国际化模块，中英文双语字典完整                                                         |
| pyproject.toml（更新） | 配置 | T-02, T-08         | 依赖已替换为 PyQt5 + pyqtgraph + numpy，版本范围适配 Ubuntu 22.04 / Python 3.10.12     |
| 环境配置与使用说明.md       | 文档 | T-09               | 针对 Ubuntu 22.04 + Python 3.10.12，使用实际路径，覆盖环境搭建、依赖安装、使用步骤、常见问题；无 usbtmc 内容 |
| 本执行计划              | 文档 | -                  | 项目执行计划.md                                                                 |

## 8. 回滚方案

### 8.1 回滚触发条件



* PyQt5/PyQtGraph 在 Ubuntu 22.04 环境中无法正常安装或运行，且 30 分钟内无法解决

* GUI 迁移后出现严重功能回归（如 NI-VISA 通信异常、数据采集错误），且无法快速定位

* 用户决定保留 Tkinter 框架，改用其他绘图方案

### 8.2 回滚步骤



1. **改造前备份（前置动作）**：在 Ubuntu 22.04 上开始改造前，先完整备份当前可用版本：



```
cp -r /home/test/max\_tool/oscill/oscill-0.1.0-linux /home/test/max\_tool/oscill/oscill-0.1.0-linux.bak
```



1. **代码回滚**：

* 若使用 Git 管理：`git revert <commit-hash>` 或 `git reset --hard <original-commit>`

* 若无 Git，从备份恢复：



```
cp -r /home/test/max\_tool/oscill/oscill-0.1.0-linux.bak/\* /home/test/max\_tool/oscill/oscill-0.1.0-linux/
```



* 恢复 `oscill/gui.py` 为原 Tkinter + matplotlib 版本

* 恢复 `pyproject.toml` 为原依赖声明（`matplotlib>=3.8`）

* 删除新增的 `oscill/i18n.py`

1. **依赖回滚（在 Ubuntu 22.04 venv 中）**：



```
cd /home/test/max\_tool/oscill/oscill-0.1.0-linux

source .venv/bin/activate

pip uninstall -y pyqtgraph PyQt5 numpy

pip install matplotlib>=3.8

\# 或重新执行：pip install -e ".\[gui]"（使用恢复后的 pyproject.toml）
```



1. **验证**：

* `python -m oscill.gui` 确认原 GUI 可启动

* `python -m oscill.cli --simulate identify` 确认 CLI 正常

* 连接示波器确认 NI-VISA 通信正常

### 8.3 回滚责任人与时间预估



* 回滚操作人：用户或开发执行者

* 预估回滚耗时：5-10 分钟（从备份恢复）

* 数据丢失风险：无（改造不涉及数据迁移，用户配置和测试结果文件不受影响）



***

## 版本变更记录



| 版本   | 日期         | 变更内容                                                                                                                                                                                                                                                                                                                               | 影响范围                                     | 操作人   |
| ---- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ----- |
| v1.0 | 2026-09-18 | 初始版本：基于项目代码全面梳理，制定 matplotlib 移除、PyQtGraph 集成、GUI 框架迁移、中英文国际化、文档交付的完整执行计划                                                                                                                                                                                                                                                          | 全部                                       | AI 生成 |
| v1.1 | 2026-09-18 | 用户确认方案后追加：①明确目标平台为 Linux + Ubuntu 22.04 LTS，代码在 Windows 编写但不在 Windows 运行测试；②将 NI-VISA 连接 / 数据采集 / 处理功能保护提升为最高优先级硬约束，明确四个底层模块零改动（仅 cli.py 允许字符串国际化）；③确认 i18n 采用轻量字典映射、运行时动态切换；④T-08 依赖检查锁定 Ubuntu 22.04 系统包具体版本（PyQt5 5.15.6、numpy 1.21.x）；⑤T-10 验证拆分为 Windows 静态验证 + Ubuntu 22.04 运行验证两阶段；⑥新增 R-10 跨平台风险、R-11 依赖错配风险、R-12 底层调用签名风险 | 3.1、3.2、3.3、T-08、T-09、T-10、风险表、校验清单、回滚方案 | AI 生成 |
| v1.2 | 2026-09-18 | 用户补充实际环境信息：①明确项目部署路径为 `/home/test/max_tool/oscill/oscill-0.1.0-linux`（venv 位于其下 `.venv`）；②明确 Python 版本为 3.10.12；③全文统一使用实际路径，T-08/T-09/T-10、回滚方案、校验清单同步更新版本与路径信息                                                                                                                                                                    | 3.1、3.3、T-08、T-09、T-10、5.3、7、8、校验清单      | AI 生成 |