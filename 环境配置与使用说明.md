# oscill 0.1.0 环境配置与使用说明（Ubuntu 22.04 + NI-VISA）

> 适用版本：oscill 0.1.0（PyQt5 + PyQtGraph 图形界面版）
> 目标平台：Ubuntu 22.04 LTS x86_64，Python 3.10.12
> 示波器：Tektronix MSO4034（通过 NI-VISA / USB 连接）
> 文档日期：2026-09-18

---

## 1. 本次改造概述

本版本在上一版（Tkinter + matplotlib 图形界面）基础上做了以下改造，**底层仪器连接、数据采集与处理逻辑（`controller.py` / `services.py` / `transport.py` / `worker.py`）完全未改动**：

| 改造项 | 旧版 | 新版 |
|-------|------|------|
| GUI 框架 | Tkinter | PyQt5 |
| 波形渲染 | matplotlib（FigureCanvasTkAgg） | PyQtGraph（GraphicsLayoutWidget） |
| 界面语言 | 仅中文 | 中文 / 英文双语，运行时可切换 |
| 国际化方案 | 无 | 轻量字典映射（`oscill/i18n.py`，非 gettext） |
| CLI 消息 | 中文硬编码 | 中文默认，`OSCILL_LANGUAGE=en_US` 可切英文 |
| 入口命令 | `oscill` / `oscill-gui` | 不变 |

> NI-VISA 连接、资源扫描、波形采集、测量、功耗统计、CSV/JSON 导出等核心功能的调用签名与旧版完全一致，仅图形界面层被替换。

---

## 2. 环境基线

### 2.1 操作系统与 Python

> **路径说明**：下文出现的 `/home/test/max_tool/oscill/oscill-0.1.0-linux` 为示例部署路径，请替换为你自己的实际项目路径。

- 操作系统：Ubuntu 22.04 LTS (Jammy Jellyfish) x86_64
- Python：系统 `python3` = **3.10.12**（Ubuntu 22.04 官方仓库版本）
- 项目根目录：`/home/test/max_tool/oscill/oscill-0.1.0-linux`
- 虚拟环境：`/home/test/max_tool/oscill/oscill-0.1.0-linux/.venv`（必须带 `--system-site-packages`，见 3.2）

### 2.2 已核实的依赖兼容组合

以下组合在 Ubuntu 22.04 + Python 3.10.12 上经过版本兼容性核对，可直接使用：

| 依赖 | 来源 | 版本 | 说明 |
|------|------|------|------|
| PyQt5 | apt `python3-pyqt5` | 5.15.6 | Ubuntu 22.04 官方仓库 `5.15.6+dfsg-1ubuntu3` |
| NumPy | apt `python3-numpy` | 1.21.5 | Ubuntu 22.04 官方仓库 `1:1.21.5-1ubuntu22.04.1` |
| pyqtgraph | pip | >=0.13, <0.14 | 纯 Python，兼容 PyQt5 5.15 / Python 3.10 |
| pyvisa | 系统已有（`/usr/lib/python3/dist-packages`） | 沿用现有版本 | NI-VISA 后端，**不要用 pip 升级**，避免与系统 NI-VISA 运行时冲突 |

**关键原则：Qt 绑定（PyQt5）和 NumPy 用 apt 安装，pyqtgraph 用 pip 安装；不要用 pip 安装/升级 PyQt5，也不要用 apt 安装 pyqtgraph。** 混用 apt 和 pip 安装 Qt 绑定是导致 `ImportError` / `Qt platform plugin` 错误的最常见原因。

pyqtgraph 0.13.x 对 NumPy 的要求为 `>=1.20`，apt 提供的 1.21.5 满足，因此 `pip install pyqtgraph` 不会强行升级系统 NumPy。

---

## 3. 环境配置步骤

以下命令均在 Ubuntu 22.04 终端中执行，假设项目已放在 `/home/test/max_tool/oscill/oscill-0.1.0-linux`。

### 3.1 安装系统依赖（apt）

```bash
sudo apt update
sudo apt install -y python3-pyqt5 python3-numpy python3-pip python3-venv
```

> `python3-pyqt5` 会自动拉入 Qt5 运行时库（`libqt5core5a`、`libqt5gui5`、`libqt5widgets5` 等）。如果需要 SVG 图标支持，可额外安装 `python3-pyqt5.qtsvg`（本项目未强制要求）。

验证 apt 包版本：

```bash
dpkg -l | grep -E 'python3-pyqt5|python3-numpy'
python3 -c "import PyQt5, numpy; print('PyQt5', PyQt5.QtCore.QT_VERSION_STR); print('NumPy', numpy.__version__)"
```

预期输出：PyQt5 绑定版本 5.15.x，NumPy 1.21.5。

### 3.2 创建虚拟环境（带系统包访问）

由于 pyvisa 和 PyQt5/NumPy 均由系统 apt 提供，虚拟环境必须允许访问系统 site-packages：

```bash
cd /home/test/max_tool/oscill/oscill-0.1.0-linux
python3 -m venv --system-site-packages .venv
source .venv/bin/activate
```

激活后提示符前会出现 `(.venv)`。验证虚拟环境能看到系统包：

```bash
python -c "import PyQt5, numpy, pyvisa; print('PyQt5 OK'); print('numpy', numpy.__version__); print('pyvisa', pyvisa.__version__)"
```

如果 `import pyvisa` 失败，说明系统 pyvisa 未安装或路径不在 `--system-site-packages` 可见范围内。本项目沿用你已验证可用的系统 pyvisa 环境，**不要在 venv 内 `pip install pyvisa`**（会与系统 NI-VISA 后端产生版本冲突）。

### 3.3 安装 pyqtgraph（pip，在 venv 内）

```bash
source /home/test/max_tool/oscill/oscill-0.1.0-linux/.venv/bin/activate
pip install "pyqtgraph>=0.13,<0.14"
```

验证：

```bash
python -c "import pyqtgraph as pg; print('pyqtgraph', pg.__version__)"
```

预期输出 0.13.x（如 0.13.7）。

### 3.4 安装 oscill 包本身

由于 pyvisa 依赖由系统提供（版本可能不满足 `pyvisa>=1.14` 的声明下限），安装 oscill 时使用 `--no-deps`，避免 pip 尝试拉取或升级 pyvisa：

```bash
cd /home/test/max_tool/oscill/oscill-0.1.0-linux
source .venv/bin/activate
pip install --no-deps -e .
```

`-e` 为可编辑安装，修改代码后无需重新安装。如果希望固定安装，去掉 `-e` 即可。

验证入口命令已注册：

```bash
which oscill oscill-gui
oscill --simulate identify
```

预期 `oscill --simulate identify` 输出 `OSCILL,SIMULATOR,0001,1.0`（模拟示波器身份字符串）。

---

## 4. 启动与基本使用

### 4.1 启动图形界面

```bash
source /home/test/max_tool/oscill/oscill-0.1.0-linux/.venv/bin/activate
oscill-gui
```

窗口标题为 `oscill`，默认尺寸 1180×800，最小尺寸 980×680。

### 4.2 命令行工具（CLI）

```bash
oscill [--resource <VISA资源名> | --simulate] [--model generic|tektronix-mso4034] <command> [选项]
```

| 命令 | 说明 |
|------|------|
| `identify` | 读取示波器身份字符串（默认命令） |
| `list-resources` | 列出当前可见的 VISA 资源 |
| `measure` | 执行一次测量 |

常用选项：

| 选项 | 说明 |
|------|------|
| `--resource` | VISA 资源名，如 `USB0::0x0699::0x0408::...::INSTR` |
| `--simulate` | 使用内置模拟示波器（无需硬件） |
| `--model` | `generic`（默认）或 `tektronix-mso4034` |
| `--parameter` | SCPI 测量参数，默认 `VPP` |
| `--source` | 测量通道，Tektronix 默认 `CH1`，generic 默认 `CHANnel1` |

示例：

```bash
# 模拟模式识别
oscill --simulate identify

# 模拟 Tektronix MSO4034 测量 CH1 峰峰值
oscill --simulate --model tektronix-mso4034 measure --parameter VPP --source CH1

# 列出真实 VISA 资源
oscill list-resources

# 连接真实示波器并识别
oscill --resource "USB0::0x0699::0x0408::C012345::INSTR" identify
```

### 4.3 模拟模式快速验证（无需示波器）

在连接真实硬件前，建议先用模拟模式确认 GUI 与依赖正常：

```bash
source .venv/bin/activate
oscill --simulate identify
oscill --simulate --model tektronix-mso4034 measure --parameter MEAN --source CH1
```

两条命令均应正常输出，无 `ImportError`。

---

## 5. 图形界面操作指南

### 5.1 界面布局

- **顶部右侧**：语言选择下拉框（中文 / English）
- **设备连接区**：资源下拉框（可手动输入）、扫描、连接、断开、身份信息
- **左侧控制面板**：自动设置 / 运行 / 停止、通道勾选、垂直量程、功耗任务与负载预设、器件信息、功耗上限、测试结果保存
- **右上测量区**：CH1–CH4 四个测量卡片（测量类型、A/V 系数、Min/Max 限值、当前值、判定状态）+ 测量轮询开关
- **右中波形区**：PyQtGraph 多通道波形图 + 单次读取 / 实时刷新 / 导出 CSV
- **右下日志区**：可折叠的操作日志

### 5.2 连接示波器

1. 点击 **扫描** 按钮，下拉框会列出当前 VISA 资源；也可直接手动输入资源名。
2. 点击 **连接**。连接成功后，身份信息栏显示示波器返回的 `*IDN?` 字符串，状态栏显示"已连接"。
3. 连接过程在后台线程执行，界面不会卡死；连接失败会弹出错误对话框。

### 5.3 通道与测量配置

- 在左侧勾选需要启用的通道（CH1–CH4）。
- 每个测量卡片的下拉框选择测量类型：平均电压、纹波峰峰值、有效值电压、最小电压、最大电压、平均电流、峰值电流、电流纹波、频率。
- 电流类通道（信号单位为 A）可编辑 **A/V 系数**（电流探头换算比），电压类通道该输入框禁用。
- 在 **Min / Max** 输入限值，用于自动判定 PASS / FAIL。
- 点击测量区下方的 **开始测量** 按钮，每 1 秒刷新一次测量值与判定状态；再次点击停止。

### 5.4 波形显示（PyQtGraph）

- **读取波形**：单次采集当前选中通道的波形并渲染。
- **实时刷新**：每 500 ms 刷新一次波形，再次点击停止。
- 多通道时，每个通道独占一行子图，共享 X 轴（时间轴），非末行隐藏底部时间刻度以节省空间。
- 每个子图显示通道名、物理量、峰峰值与采样点数；Y 轴自动使用工程单位（mV / V / kV 等）。
- **导出 CSV**：将当前波形数据保存为 CSV 文件。

### 5.5 功耗统计与测试结果

- 左侧选择 **功耗任务**（POWER-01B 至 POWER-06）和 **负载**（Idle / Full Load / Read / Write 等），点击 **应用预设** 可自动配置通道与测量类型。
- 同时存在电压通道与电流通道时，测量轮询会同步采集功率窗口，累计平均功率、峰值功率与能耗（Wh）。
- 在 **功耗上限** 输入限值后，总体判定会综合通道测量与功耗峰值。
- 填写 **器件型号** 与 **固件版本**，点击 **保存测试结果** 导出 JSON 报告（含测量值、功耗统计、限值、判定结果与时间戳）。

### 5.6 语言切换

- 界面顶部右侧下拉框选择 **中文** 或 **English**，**无需重启**，所有标签、按钮、状态提示、测量卡片、波形标题立即切换。
- 测量类型与功耗任务名称也会随语言切换；JSON 导出中的 `measurement_type` 字段使用当前界面语言。
- 功耗负载名称（Idle / Full Load 等）保持英文，因为它们是标准测试术语且会原样写入 JSON。

---

## 6. 国际化（i18n）说明

### 6.1 实现方式

- 采用轻量字典映射，位于 `oscill/i18n.py`，不使用 gettext，无需编译 `.mo` 文件。
- 中英文字典各 151 条文案，键完全对称；缺译时自动回退中文，再回退原始键，不会崩溃。
- `Translator` 类支持 `on_language_changed()` 注册回调，GUI 在切换语言时统一重绘所有控件。
- 运行时动态切换，无需重启应用。

### 6.2 CLI 语言

CLI 默认中文（保持原行为），可通过环境变量切换英文：

```bash
OSCILL_LANGUAGE=en_US oscill list-resources
OSCILL_LANGUAGE=zh_CN oscill --simulate identify
```

仅支持 `zh_CN` 与 `en_US` 两个值，其他值回退中文。

### 6.3 自检脚本

项目根目录提供两个零依赖自检脚本（仅用 Python 标准库，可在 Ubuntu 上直接运行）：

```bash
cd /home/test/max_tool/oscill/oscill-0.1.0-linux
python _static_check.py    # 检查所有 tr() 键是否存在、中英字典是否对称、self 属性是否完整
python _format_check.py    # 检查 {占位符} 在中英文与调用处是否一致
```

两个脚本均应输出 `OK`。修改文案或新增界面元素后建议重跑。

---

## 7. 真机回归验证清单（阶段 B）

以下步骤需在 Ubuntu 22.04 + 真实 Tektronix MSO4034 上执行，用于确认改造未破坏原有功能：

### 7.1 连接与采集

- [ ] `oscill list-resources` 能列出示波器 USB 资源
- [ ] `oscill --resource <资源名> identify` 返回正确的 `*IDN?` 字符串
- [ ] GUI 点击扫描 → 连接，身份信息正确显示
- [ ] GUI 点击运行 / 停止 / 自动设置，示波器响应正常

### 7.2 测量

- [ ] 勾选 CH1，选择"平均电压"，开始测量，数值合理刷新
- [ ] 切换测量类型（VPP / RMS / MIN / MAX / FREQUENCY），数值与单位正确
- [ ] 配置电流通道（A/V 系数），电流测量值正确
- [ ] 输入 Min/Max 限值，PASS / FAIL 判定正确（含低于下限、高于上限、无信号三种情况）

### 7.3 波形

- [ ] 点击"读取波形"，PyQtGraph 正确渲染单通道波形
- [ ] 同时勾选 CH1+CH2，两行子图共享 X 轴，颜色分别为蓝/橙
- [ ] 点击"实时刷新"，波形每 500 ms 平滑更新，界面不卡死
- [ ] 导出 CSV，文件内容包含时间与电压列

### 7.4 功耗与导出

- [ ] 选择 POWER-05 / POWER-06，应用预设自动配置 CH1 电压 + CH2 电流
- [ ] 开始测量后，平均功率、峰值功率、能耗（Wh）累计正确
- [ ] 输入功耗上限，超限判定为 FAIL
- [ ] 保存 JSON 报告，字段完整（test_id / measurements / power / limits / result）

### 7.5 国际化

- [ ] 运行中切换中文 → 英文 → 中文，所有界面元素正确重绘，无残留中文/英文
- [ ] 切换语言后测量数值与波形不丢失
- [ ] `OSCILL_LANGUAGE=en_US oscill` 输出英文消息，默认输出中文

### 7.6 与改造前对比

- [ ] 同一通道同一信号下，测量数值与旧版（matplotlib 版）一致（误差在示波器精度内）
- [ ] 波形 CSV 数据与旧版一致
- [ ] JSON 报告结构与旧版兼容

---

## 8. 常见问题（FAQ）

**Q1：启动 `oscill-gui` 报 `ImportError: No module named 'PyQt5'`**
A：未激活带 `--system-site-packages` 的 venv，或未安装 `python3-pyqt5`。执行 `source .venv/bin/activate` 并确认 `python -c "import PyQt5"` 成功。

**Q2：报 `qt.qpa.plugin: Could not load the Qt platform plugin "xcb"`**
A：缺少 Qt X11 平台插件。执行 `sudo apt install libqt5x11extras5 libxkbcommon-x11-0`，或在纯无显示环境（SSH 无 X11 转发）下设置 `QT_QPA_PLATFORM=offscreen`（仅用于无头测试，不能实际操作界面）。

**Q3：`pip install pyqtgraph` 后 NumPy 被升级到 2.x，导致其他包报错**
A：pyqtgraph 0.13.x 不要求 NumPy 2.x。如果 pip 仍尝试升级，使用 `pip install --no-deps "pyqtgraph>=0.13,<0.14"`，并确认 apt 的 `python3-numpy`（1.21.5）已安装且 venv 带 `--system-site-packages`。

**Q4：`pip install -e .` 尝试安装/升级 pyvisa 并失败**
A：使用 `pip install --no-deps -e .`。pyvisa 由系统 apt/已有环境提供，本项目不强制通过 pip 管理。

**Q5：连接示波器超时或报 VI_ERROR**
A：确认 NI-VISA 运行时已安装且示波器 USB 连接正常；`oscill list-resources` 应能看到资源。本项目沿用你已验证可用的 NI-VISA 连接方式，未改动传输层。

**Q6：波形实时刷新时界面卡顿**
A：实时刷新间隔为 500 ms，采集在后台线程执行，UI 线程仅做 `setData` 更新。若卡顿，检查是否同时开启了测量轮询（1000 ms）与波形轮询（500 ms），两者并发是正常的；可适当减少同时采集的通道数。

**Q7：如何完全卸载并回退到旧版（matplotlib）**
A：见第 9 节回滚方案。

---

## 9. 回滚方案

如果新版 GUI 在真机上出现无法快速修复的问题，可按以下步骤回退：

### 9.1 备份当前版本

```bash
cd /home/test/max_tool/oscill
cp -r oscill-0.1.0-linux oscill-0.1.0-linux.pyqt5-backup-$(date +%Y%m%d)
```

### 9.2 恢复旧版代码

将旧版（Tkinter + matplotlib）的 `oscill/gui.py`、`oscill/cli.py`、`pyproject.toml` 覆盖回项目目录，并删除 `oscill/i18n.py`。如果你保留了旧版备份目录，直接：

```bash
cp <旧版备份>/oscill/gui.py oscill-0.1.0-linux/oscill/gui.py
cp <旧版备份>/oscill/cli.py oscill-0.1.0-linux/oscill/cli.py
cp <旧版备份>/pyproject.toml oscill-0.1.0-linux/pyproject.toml
rm oscill-0.1.0-linux/oscill/i18n.py
```

### 9.3 切换依赖

```bash
source oscill-0.1.0-linux/.venv/bin/activate
pip uninstall -y pyqtgraph
sudo apt install -y python3-matplotlib   # 旧版 GUI 依赖
pip install --no-deps -e oscill-0.1.0-linux
```

> 底层模块（controller / services / transport / worker）新旧版完全一致，回滚时无需改动。

---

## 10. 文件清单

本次改造涉及/新增的文件：

| 文件 | 变更 | 说明 |
|------|------|------|
| `oscill/gui.py` | 重写 | PyQt5 + PyQtGraph + i18n，替代 Tkinter + matplotlib |
| `oscill/i18n.py` | 新增 | 轻量国际化模块，中英文字典与运行时切换 |
| `oscill/cli.py` | 小幅修改 | 两处消息国际化，命令逻辑未动 |
| `pyproject.toml` | 修改 | `gui` / `dev` 依赖由 matplotlib 改为 PyQt5 + pyqtgraph + numpy |
| `oscill/controller.py` | 未改动 | 仪器控制层 |
| `oscill/services.py` | 未改动 | 采集/测量/功耗服务层 |
| `oscill/transport.py` | 未改动 | VISA 传输层 |
| `oscill/worker.py` | 未改动 | 后台串行执行器 |
| `_static_check.py` | 新增 | i18n 键与属性自检（零依赖） |
| `_format_check.py` | 新增 | 格式化占位符一致性自检（零依赖） |
| `项目执行计划.md` | 已有 | 项目执行计划 v1.2 |
| `环境配置与使用说明.md` | 新增 | 本文档 |

---

*如有问题，先运行第 6.3 节的两个自检脚本确认代码完整性，再按第 7 节清单逐项在真机上验证。*
